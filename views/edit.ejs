<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex flex-col justify-center items-center px-4">
    <!-- Form Section -->
    <div class="bg-zinc-800 p-8 rounded-2xl shadow-xl w-full max-w-lg mb-6">
        <form action="/edit" method="post">
            <label class="block text-base font-semibold mb-2"> Name</label>

            <!-- Displayed (readonly) -->
            <input type="text" value="<%= filename %>" placeholder="Previous Name" readonly
                class="w-full px-4 py-3 bg-zinc-700 text-white rounded-lg outline-none mb-2" />

            <!-- Hidden input to actually send the filename -->
            <input type="hidden" name="Previous" value="<%= filename %>" />



            <label class="block text-base font-semibold mb-2">New Name</label>
            <textarea name="newName" placeholder="New Name" rows="2" required
                class="w-full px-4 py-3 bg-zinc-700 text-white rounded-lg outline-none resize-none focus:ring-2 focus:ring-blue-500 transition mb-4"></textarea>

            <input type="submit" value="Update Name"
                class="w-full py-3 bg-yellow-700 hover:bg-yellow-600 transition rounded-lg font-medium cursor-pointer" />
        </form>
    </div>

    <a href="/" class="text-blue-400 hover:underline mt-2">← Back to Home</a>
</body>

</html>